# 5. 功能需求
> **按照FPA**《集中故障管理系统-监控综合应用-关于集团事件工单省部接口数据上报保障的开发需求-15400_20250318-FPA预估2025版V3_厂家版本.xlsx》“2. 规模估算”生成，仅保留EI/EO/EQ类功能点，剔除ILF/EIF类

## 5.1. 监控管理应用（注：FPA一级分类）
### 5.1.1. 事件工单管理（注：FPA二级分类）
#### 5.1.1.1. 事件工单管理场景（注：FPA三级分类）
##### 5.1.1.1.1. 事件处理类工单流程（注：FPA功能点名称）

###### 5.1.1.1.01 瞬断故障场景识别（注：FPA功能点计数项）
**功能描述：** 定时读取工单数据及瞬断故障场景规则，按“故障时长<5分钟且故障类型='瞬断'”等规则进行匹配，生成瞬断故障场景识别结果并关联至工单数据。
**系统界面：** 无
**输入：** 瞬断故障数据（工单数据、故障数据）、瞬断故障识别规则、相关字典表（故障类型、区域信息）
**输出：** 瞬断故障场景识别结果（场景类型、匹配规则ID、置信度），更新至对应数据表
**处理过程：** 1. 读取瞬断故障相关基础数据；2. 加载瞬断故障识别规则；3. 按规则匹配数据，判定场景类型；4. 生成场景识别结果，更新至数据表
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：回单模板表（如适用）
本期涉及原有但没修改的内部逻辑文件：事件工单主表、故障类型字典表、区域字典表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.02 瞬断故障场景回单内容动态匹配（注：FPA功能点计数项）
**功能描述：** 根据已识别的瞬断故障场景，匹配对应的回单内容模板，填充工单关键信息（故障时长、处理结果），生成标准化回单内容。
**系统界面：** 无
**输入：** 已识别的故障场景信息、回单模板库数据、工单关键信息（故障处理结果、耗时）
**输出：** 动态匹配的回单内容、模板匹配结果（模板ID、匹配度），更新工单回单字段
**处理过程：** 1. 提取已识别的故障场景信息；2. 筛选适用的回单模板；3. 按模板优先级匹配最优模板；4. 填充模板变量生成回单内容；5. 更新工单回单字段
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：回单模板表（如适用）
本期涉及原有但没修改的内部逻辑文件：事件工单主表、故障类型字典表、区域字典表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.03 大面积故障场景识别（注：FPA功能点计数项）
**功能描述：** 分析工单故障影响范围、涉及用户数等数据，按“影响用户数>1000或影响区域>3个区县”等规则，识别大面积故障场景并标记场景类型。
**系统界面：** 无
**输入：** 大面积故障数据（工单数据、故障数据）、大面积故障识别规则、相关字典表（故障类型、区域信息）
**输出：** 大面积故障场景识别结果（场景类型、匹配规则ID、置信度），更新至对应数据表
**处理过程：** 1. 读取大面积故障相关基础数据；2. 加载大面积故障识别规则；3. 按规则匹配数据，判定场景类型；4. 生成场景识别结果，更新至数据表
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：回单模板表（如适用）
本期涉及原有但没修改的内部逻辑文件：事件工单主表、故障类型字典表、区域字典表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.04 大面积故障场景回单内容动态匹配（注：FPA功能点计数项）
**功能描述：** 基于大面积故障场景类型，动态匹配回单模板库中的对应模板，整合故障处理措施、恢复时间等信息，生成回单内容。
**系统界面：** 无
**输入：** 已识别的故障场景信息、回单模板库数据、工单关键信息（故障处理结果、耗时）
**输出：** 动态匹配的回单内容、模板匹配结果（模板ID、匹配度），更新工单回单字段
**处理过程：** 1. 提取已识别的故障场景信息；2. 筛选适用的回单模板；3. 按模板优先级匹配最优模板；4. 填充模板变量生成回单内容；5. 更新工单回单字段
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：回单模板表（如适用）
本期涉及原有但没修改的内部逻辑文件：事件工单主表、故障类型字典表、区域字典表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.05 虚拟化故障场景识别（注：FPA功能点计数项）
**功能描述：** 提取虚拟化设备故障数据（如虚拟机宕机、资源不足），按虚拟化故障特征规则匹配，识别虚拟化故障场景并分类。
**系统界面：** 无
**输入：** 虚拟化故障数据（工单数据、故障数据）、虚拟化故障识别规则、相关字典表（故障类型、区域信息）
**输出：** 虚拟化故障场景识别结果（场景类型、匹配规则ID、置信度），更新至对应数据表
**处理过程：** 1. 读取虚拟化故障相关基础数据；2. 加载虚拟化故障识别规则；3. 按规则匹配数据，判定场景类型；4. 生成场景识别结果，更新至数据表
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：回单模板表（如适用）
本期涉及原有但没修改的内部逻辑文件：事件工单主表、故障类型字典表、区域字典表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.06 虚拟化故障场景回单内容动态匹配（注：FPA功能点计数项）
**功能描述：** 针对虚拟化故障场景分类，匹配专属回单模板，提取虚拟化设备信息、故障恢复情况，生成标准化回单内容。
**系统界面：** 无
**输入：** 已识别的故障场景信息、回单模板库数据、工单关键信息（故障处理结果、耗时）
**输出：** 动态匹配的回单内容、模板匹配结果（模板ID、匹配度），更新工单回单字段
**处理过程：** 1. 提取已识别的故障场景信息；2. 筛选适用的回单模板；3. 按模板优先级匹配最优模板；4. 填充模板变量生成回单内容；5. 更新工单回单字段
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：回单模板表（如适用）
本期涉及原有但没修改的内部逻辑文件：事件工单主表、故障类型字典表、区域字典表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.07 T2现场和非现场故障处理场景识别（注：FPA功能点计数项）
**功能描述：** 根据工单处理方式（现场/非现场）、故障类型、处理时限等信息，识别T2故障处理场景，为后续模板匹配提供场景依据。
**系统界面：** 无
**输入：** T2现场和非现场故障处理数据（工单数据、故障数据）、T2现场和非现场故障处理识别规则、相关字典表（故障类型、区域信息）
**输出：** T2现场和非现场故障处理场景识别结果（场景类型、匹配规则ID、置信度），更新至对应数据表
**处理过程：** 1. 读取T2现场和非现场故障处理相关基础数据；2. 加载T2现场和非现场故障处理识别规则；3. 按规则匹配数据，判定场景类型；4. 生成场景识别结果，更新至数据表
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：回单模板表（如适用）
本期涉及原有但没修改的内部逻辑文件：事件工单主表、故障类型字典表、区域字典表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.08 T2现场和非现场多模板优先级匹配（注：FPA功能点计数项）
**功能描述：** 针对T2现场和非现场故障处理场景，匹配多个适用的回单模板，按模板优先级（如现场模板优先级高于非现场模板）选择最优模板。
**系统界面：** 无
**输入：** T2现场和非现场多模板优先级匹配相关输入数据、业务规则配置、基础字典表数据
**输出：** T2现场和非现场多模板优先级匹配处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收T2现场和非现场多模板优先级匹配相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.09 T2现场和非现场回单（注：FPA功能点计数项）
**功能描述：** 根据T2现场和非现场故障处理结果，使用匹配的回单模板生成回单内容，提交回单并更新工单状态。
**系统界面：** 无
**输入：** T2现场和非现场回单相关输入数据、业务规则配置、基础字典表数据
**输出：** T2现场和非现场回单处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收T2现场和非现场回单相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.10 T1受理时间计算（注：FPA功能点计数项）
**功能描述：** 提取工单创建时间和T1受理操作时间，计算T1受理耗时（受理时间-创建时间），判定是否符合受理时限要求。
**系统界面：** 无
**输入：** 工单时间数据（创建时间、受理时间、处理时间）、时间计算规则、标准时限配置
**输出：** 时间计算结果（耗时、超时标识）、时间统计报表，更新工单时间字段
**处理过程：** 1. 提取工单相关时间数据；2. 按时间计算规则计算耗时；3. 对比标准时限判定超时；4. 生成时间计算结果；5. 更新工单时间字段及统计报表
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 4 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、操作人信息表、时间统计报表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.11 T1受理操作人匹配（注：FPA功能点计数项）
**功能描述：** 根据T1受理操作记录，匹配操作人信息（姓名、工号、所属班组），关联至工单受理记录。
**系统界面：** 无
**输入：** 操作日志数据（操作时间、操作类型）、操作人信息表（姓名、工号、班组）、工单数据
**输出：** 匹配的操作人信息（姓名、工号、班组）、操作人关联记录，更新工单操作人字段
**处理过程：** 1. 提取操作日志数据；2. 匹配操作人信息表；3. 获取操作人详细信息；4. 关联操作人与工单；5. 更新工单操作人字段
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 4 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、操作人信息表、时间统计报表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.12 T1移交T2时间计算（注：FPA功能点计数项）
**功能描述：** 获取T1移交操作时间和T2接收时间，计算T1移交T2耗时，记录超时情况并生成时间统计数据。
**系统界面：** 无
**输入：** 工单时间数据（创建时间、受理时间、处理时间）、时间计算规则、标准时限配置
**输出：** 时间计算结果（耗时、超时标识）、时间统计报表，更新工单时间字段
**处理过程：** 1. 提取工单相关时间数据；2. 按时间计算规则计算耗时；3. 对比标准时限判定超时；4. 生成时间计算结果；5. 更新工单时间字段及统计报表
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 4 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、操作人信息表、时间统计报表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.13 T1移交T2操作人处理（注：FPA功能点计数项）
**功能描述：** 处理T1移交T2操作人处理相关业务逻辑，接收输入数据，按预设规则处理，生成对应输出结果，支撑相关业务流程。
**系统界面：** 无
**输入：** T1移交T2操作人处理相关输入数据、业务规则配置、基础字典表数据
**输出：** T1移交T2操作人处理处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收T1移交T2操作人处理相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.14 故障初步处理情况定界定位处理（注：FPA功能点计数项）
**功能描述：** 提取故障初步处理信息（如故障位置、影响范围、处理措施），进行故障定界定位处理，生成初步定界定位结果。
**系统界面：** 无
**输入：** 故障初步处理情况定界定位处理相关输入数据、业务规则配置、基础字典表数据
**输出：** 故障初步处理情况定界定位处理处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收故障初步处理情况定界定位处理相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.15 T2受理操作时间处理（注：FPA功能点计数项）
**功能描述：** 处理T2受理操作时间处理相关业务逻辑，接收输入数据，按预设规则处理，生成对应输出结果，支撑相关业务流程。
**系统界面：** 无
**输入：** T2受理操作时间处理相关输入数据、业务规则配置、基础字典表数据
**输出：** T2受理操作时间处理处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收T2受理操作时间处理相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.16 T2受理操作人匹配（注：FPA功能点计数项）
**功能描述：** 提取T2受理操作日志，匹配操作人基本信息，关联至T2受理记录并更新工单操作人字段。
**系统界面：** 无
**输入：** 操作日志数据（操作时间、操作类型）、操作人信息表（姓名、工号、班组）、工单数据
**输出：** 匹配的操作人信息（姓名、工号、班组）、操作人关联记录，更新工单操作人字段
**处理过程：** 1. 提取操作日志数据；2. 匹配操作人信息表；3. 获取操作人详细信息；4. 关联操作人与工单；5. 更新工单操作人字段
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 4 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、操作人信息表、时间统计报表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.17 T1和T2回单操作时间计算（注：FPA功能点计数项）
**功能描述：** 统计T1和T2回单操作的开始时间和结束时间，计算回单操作耗时，生成回单效率统计报表。
**系统界面：** 无
**输入：** 工单时间数据（创建时间、受理时间、处理时间）、时间计算规则、标准时限配置
**输出：** 时间计算结果（耗时、超时标识）、时间统计报表，更新工单时间字段
**处理过程：** 1. 提取工单相关时间数据；2. 按时间计算规则计算耗时；3. 对比标准时限判定超时；4. 生成时间计算结果；5. 更新工单时间字段及统计报表
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 4 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、操作人信息表、时间统计报表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.18 T1和T2回单操作操作人匹配（注：FPA功能点计数项）
**功能描述：** 根据回单操作日志，匹配T1和T2回单操作人信息，记录操作人及操作时间，生成操作人工作统计。
**系统界面：** 无
**输入：** 操作日志数据（操作时间、操作类型）、操作人信息表（姓名、工号、班组）、工单数据
**输出：** 匹配的操作人信息（姓名、工号、班组）、操作人关联记录，更新工单操作人字段
**处理过程：** 1. 提取操作日志数据；2. 匹配操作人信息表；3. 获取操作人详细信息；4. 关联操作人与工单；5. 更新工单操作人字段
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 4 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、操作人信息表、时间统计报表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.19 采取措施时间计算规则（注：FPA功能点计数项）
**功能描述：** 处理采取措施时间计算规则相关业务逻辑，接收输入数据，按预设规则处理，生成对应输出结果，支撑相关业务流程。
**系统界面：** 无
**输入：** 工单时间数据（创建时间、受理时间、处理时间）、时间计算规则、标准时限配置
**输出：** 时间计算结果（耗时、超时标识）、时间统计报表，更新工单时间字段
**处理过程：** 1. 提取工单相关时间数据；2. 按时间计算规则计算耗时；3. 对比标准时限判定超时；4. 生成时间计算结果；5. 更新工单时间字段及统计报表
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 4 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、操作人信息表、时间统计报表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.20 采取措施时间瞬断场景、现场处理场景处理（注：FPA功能点计数项）
**功能描述：** 针对瞬断故障场景和现场处理场景，按预设计算规则计算采取措施时间，记录计算结果并关联工单。
**系统界面：** 无
**输入：** 采取措施时间瞬断场景、现场处理场景处理相关输入数据、业务规则配置、基础字典表数据
**输出：** 采取措施时间瞬断场景、现场处理场景处理处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收采取措施时间瞬断场景、现场处理场景处理相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.21 采取措施时间非现场处理T1和T2受理时间处理（注：FPA功能点计数项）
**功能描述：** 对非现场处理的工单，结合T1和T2受理时间，计算采取措施时间，生成非现场处理时间统计数据。
**系统界面：** 无
**输入：** 采取措施时间非现场处理T1和T2受理时间处理相关输入数据、业务规则配置、基础字典表数据
**输出：** 采取措施时间非现场处理T1和T2受理时间处理处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收采取措施时间非现场处理T1和T2受理时间处理相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.22 匹配回单模板是否到达现场判断处理（注：FPA功能点计数项）
**功能描述：** 判断工单处理是否需要到达现场，根据判断结果匹配对应的回单模板（现场处理模板/非现场处理模板）。
**系统界面：** 无
**输入：** 匹配回单模板是否到达现场判断处理相关输入数据、业务规则配置、基础字典表数据
**输出：** 匹配回单模板是否到达现场判断处理处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收匹配回单模板是否到达现场判断处理相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.23 到达现场时间计算（注：FPA功能点计数项）
**功能描述：** 根据工单派单时间和现场人员到达时间，计算到达现场耗时，对比标准时限判定是否超时。
**系统界面：** 无
**输入：** 工单时间数据（创建时间、受理时间、处理时间）、时间计算规则、标准时限配置
**输出：** 时间计算结果（耗时、超时标识）、时间统计报表，更新工单时间字段
**处理过程：** 1. 提取工单相关时间数据；2. 按时间计算规则计算耗时；3. 对比标准时限判定超时；4. 生成时间计算结果；5. 更新工单时间字段及统计报表
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 4 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、操作人信息表、时间统计报表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.24 排查时间计算（注：FPA功能点计数项）
**功能描述：** 提取故障排查开始时间和结束时间，计算故障排查耗时，关联故障类型生成排查效率分析数据。
**系统界面：** 无
**输入：** 工单时间数据（创建时间、受理时间、处理时间）、时间计算规则、标准时限配置
**输出：** 时间计算结果（耗时、超时标识）、时间统计报表，更新工单时间字段
**处理过程：** 1. 提取工单相关时间数据；2. 按时间计算规则计算耗时；3. 对比标准时限判定超时；4. 生成时间计算结果；5. 更新工单时间字段及统计报表
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 4 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、操作人信息表、时间统计报表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.25 故障消除时间动态赋值（注：FPA功能点计数项）
**功能描述：** 根据故障恢复确认时间，动态赋值工单的故障消除时间字段，同步更新工单状态为“已恢复”。
**系统界面：** 无
**输入：** 故障消除时间动态赋值相关输入数据、业务规则配置、基础字典表数据
**输出：** 故障消除时间动态赋值处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收故障消除时间动态赋值相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.26 是否实际影响业务赋值（注：FPA功能点计数项）
**功能描述：** 基于故障影响范围、业务中断情况，判定是否实际影响业务，为工单“是否影响业务”字段赋值。
**系统界面：** 无
**输入：** 是否实际影响业务赋值相关输入数据、业务规则配置、基础字典表数据
**输出：** 是否实际影响业务赋值处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收是否实际影响业务赋值相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.27 影响业务类型判定（注：FPA功能点计数项）
**功能描述：** 分析故障影响的业务系统（如语音、数据、视频），判定影响业务类型，为工单“影响业务类型”字段赋值。
**系统界面：** 无
**输入：** 影响业务类型判定相关输入数据、业务规则配置、基础字典表数据
**输出：** 影响业务类型判定处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收影响业务类型判定相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.28 业务恢复时间赋值（注：FPA功能点计数项）
**功能描述：** 提取业务恢复确认时间，为工单“业务恢复时间”字段赋值，关联故障消除时间计算业务中断时长。
**系统界面：** 无
**输入：** 业务恢复时间赋值相关输入数据、业务规则配置、基础字典表数据
**输出：** 业务恢复时间赋值处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收业务恢复时间赋值相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.29 故障处理结果赋值（注：FPA功能点计数项）
**功能描述：** 根据故障处理情况（成功恢复/未恢复/转派），为工单“处理结果”字段赋值，生成处理结果统计。
**系统界面：** 无
**输入：** 故障处理结果赋值相关输入数据、业务规则配置、基础字典表数据
**输出：** 故障处理结果赋值处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收故障处理结果赋值相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.30 故障原因分类匹配（注：FPA功能点计数项）
**功能描述：** 处理故障原因分类匹配相关业务逻辑，接收输入数据，按预设规则处理，生成对应输出结果，支撑相关业务流程。
**系统界面：** 无
**输入：** 故障原因分类匹配相关输入数据、业务规则配置、基础字典表数据
**输出：** 故障原因分类匹配处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收故障原因分类匹配相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.31 处理措施信息处理（注：FPA功能点计数项）
**功能描述：** 收集故障处理过程中采取的措施信息（如重启设备、更换部件、配置调整），整理为标准化处理措施记录，关联至工单。
**系统界面：** 无
**输入：** 处理措施信息处理相关输入数据、业务规则配置、基础字典表数据
**输出：** 处理措施信息处理处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收处理措施信息处理相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.32 新增同源追单规则配置（注：FPA功能点计数项）
**功能描述：** 提供同源追单规则新增功能，配置追单条件（故障类型、影响范围、时间窗口）、追单触发方式，保存规则配置。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 新增的规则配置数据（规则ID、配置参数、启用状态），入库至对应规则表
**处理过程：** 1. 接收规则配置参数；2. 校验参数合法性及操作人权限；3. 生成规则ID；4. 保存规则配置至规则表；5. 记录新增日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：新增同源追单规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.33 修改同源追单规则配置（注：FPA功能点计数项）
**功能描述：** 支持同源追单规则的修改操作，更新追单条件、触发方式等配置信息，保存修改并记录变更日志。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 修改后的规则配置数据、规则变更日志，更新规则表对应记录
**处理过程：** 1. 接收规则修改请求及参数；2. 校验权限及参数合法性；3. 定位规则表对应记录；4. 更新规则配置参数；5. 记录修改日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：修改同源追单规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.34 查询同源追单规则配置（注：FPA功能点计数项）
**功能描述：** 接收同源追单规则查询条件（规则ID、规则名称、启用状态），筛选规则配置数据，分页展示查询结果。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 匹配的规则配置列表，支持分页展示
**处理过程：** 1. 接收查询条件；2. 校验查询权限；3. 筛选规则表符合条件记录；4. 按指定顺序排序；5. 生成分页查询结果
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：查询同源追单规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.35 同源追单条件判定（注：FPA功能点计数项）
**功能描述：** 根据工单故障特征（故障类型、影响区域、设备类型），判定是否符合同源追单条件，生成追单建议。
**系统界面：** 无
**输入：** 同源追单条件判定相关输入数据、业务规则配置、基础字典表数据
**输出：** 同源追单条件判定处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收同源追单条件判定相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.36 新增工单合并规则配置（注：FPA功能点计数项）
**功能描述：** 提供工单合并规则新增功能，配置合并条件（故障类型、时间范围、影响区域）、合并优先级，保存规则配置。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 新增的规则配置数据（规则ID、配置参数、启用状态），入库至对应规则表
**处理过程：** 1. 接收规则配置参数；2. 校验参数合法性及操作人权限；3. 生成规则ID；4. 保存规则配置至规则表；5. 记录新增日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：新增工单合并规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.37 修改工单合并规则配置（注：FPA功能点计数项）
**功能描述：** 支持工单合并规则的修改操作，更新合并条件、优先级等配置信息，保存修改并记录规则变更日志。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 修改后的规则配置数据、规则变更日志，更新规则表对应记录
**处理过程：** 1. 接收规则修改请求及参数；2. 校验权限及参数合法性；3. 定位规则表对应记录；4. 更新规则配置参数；5. 记录修改日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：修改工单合并规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.38 查询工单合并规则配置（注：FPA功能点计数项）
**功能描述：** 接收工单合并规则查询条件（规则ID、规则类型、启用状态），筛选规则数据，分页展示查询结果。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 匹配的规则配置列表，支持分页展示
**处理过程：** 1. 接收查询条件；2. 校验查询权限；3. 筛选规则表符合条件记录；4. 按指定顺序排序；5. 生成分页查询结果
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：查询工单合并规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.39 无线工单合并优先级别判定（注：FPA功能点计数项）
**功能描述：** 分析无线类工单的故障紧急程度、影响用户数，判定工单合并优先级，为工单合并提供依据。
**系统界面：** 无
**输入：** 无线工单合并优先级别判定相关输入数据、业务规则配置、基础字典表数据
**输出：** 无线工单合并优先级别判定处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收无线工单合并优先级别判定相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.40 无线工单合并时间判定（注：FPA功能点计数项）
**功能描述：** 根据无线工单的创建时间、故障持续时间，判定是否符合合并时间窗口要求，决定是否合并工单。
**系统界面：** 无
**输入：** 无线工单合并时间判定相关输入数据、业务规则配置、基础字典表数据
**输出：** 无线工单合并时间判定处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收无线工单合并时间判定相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.41 传输工单合并类型判定（注：FPA功能点计数项）
**功能描述：** 识别传输类工单的故障类型（如光缆中断、设备故障），判定工单合并类型，匹配对应的合并规则。
**系统界面：** 无
**输入：** 传输工单合并类型判定相关输入数据、业务规则配置、基础字典表数据
**输出：** 传输工单合并类型判定处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收传输工单合并类型判定相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.42 传输工单合并时间判定（注：FPA功能点计数项）
**功能描述：** 分析传输工单的故障发生时间和处理进度，判定是否在合并时间范围内，生成合并判定结果。
**系统界面：** 无
**输入：** 传输工单合并时间判定相关输入数据、业务规则配置、基础字典表数据
**输出：** 传输工单合并时间判定处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收传输工单合并时间判定相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.43 主子工单关联匹配及呈现（注：FPA功能点计数项）
**功能描述：** 匹配主工单与子工单的关联关系，按主工单ID聚合子工单信息，以层级结构呈现主子工单关联数据。
**系统界面：** 无
**输入：** 主子工单关联匹配及呈现相关输入数据、业务规则配置、基础字典表数据
**输出：** 主子工单关联匹配及呈现处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收主子工单关联匹配及呈现相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.44 工单详情督办次数统计（注：FPA功能点计数项）
**功能描述：** 按工单ID统计督办次数、督办时间、督办人信息，生成工单督办次数统计结果，关联工单详情展示。
**系统界面：** 无
**输入：** 统计维度条件（时间范围、专业类型、工单状态）、基础数据（工单数据、上报数据）、统计规则
**输出：** 统计结果报表（表格/图表）、统计数据明细，入库至对应统计报表
**处理过程：** 1. 接收工单详情督办次数统计相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：工单详情督办次数统计报表
本期涉及原有但没修改的内部逻辑文件：事件工单主表、基础字典表、权限表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.45 工单督办记录列表呈现（注：FPA功能点计数项）
**功能描述：** 按工单督办记录的时间顺序，分页呈现督办记录列表，包含督办ID、工单ID、督办人、督办内容、督办时间。
**系统界面：** 无
**输入：** 工单督办记录列表呈现相关输入数据、业务规则配置、基础字典表数据
**输出：** 工单督办记录列表呈现处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收工单督办记录列表呈现相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.46 工单督办记录快速查询（注：FPA功能点计数项）
**功能描述：** 接收工单督办记录查询条件（工单ID、督办人、时间范围），快速筛选督办记录，返回查询结果列表。
**系统界面：** 无
**输入：** 查询条件（ID、时间范围、状态）、基础数据表、权限校验信息
**输出：** 查询结果列表（含关键字段），支持分页展示、导出
**处理过程：** 1. 接收工单督办记录快速查询相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：工单督办记录快速统计报表
本期涉及原有但没修改的内部逻辑文件：事件工单主表、基础字典表、权限表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.47 待办工单督办记录列表呈现（注：FPA功能点计数项）
**功能描述：** 按工单督办记录的时间顺序，分页呈现督办记录列表，包含督办ID、工单ID、督办人、督办内容、督办时间。
**系统界面：** 无
**输入：** 待办工单督办记录列表呈现相关输入数据、业务规则配置、基础字典表数据
**输出：** 待办工单督办记录列表呈现处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收待办工单督办记录列表呈现相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.48 待办工单督办记录快速查询（注：FPA功能点计数项）
**功能描述：** 接收工单督办记录查询条件（工单ID、督办人、时间范围），快速筛选督办记录，返回查询结果列表。
**系统界面：** 无
**输入：** 查询条件（ID、时间范围、状态）、基础数据表、权限校验信息
**输出：** 查询结果列表（含关键字段），支持分页展示、导出
**处理过程：** 1. 接收待办工单督办记录快速查询相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：待办工单督办记录快速统计报表
本期涉及原有但没修改的内部逻辑文件：事件工单主表、基础字典表、权限表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.49 集团勘误移交勘误分类判定（注：FPA功能点计数项）
**功能描述：** 根据集团勘误内容，判定勘误分类（如数据错误、流程错误、配置错误），为勘误移交提供分类依据。
**系统界面：** 无
**输入：** 集团勘误移交勘误分类判定相关输入数据、业务规则配置、基础字典表数据
**输出：** 集团勘误移交勘误分类判定处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收集团勘误移交勘误分类判定相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.50 集团勘误移交原因说明校验（注：FPA功能点计数项）
**功能描述：** 校验集团勘误移交原因说明的完整性、规范性，判定是否符合移交要求，返回校验结果。
**系统界面：** 无
**输入：** 集团勘误移交原因说明校验相关输入数据、业务规则配置、基础字典表数据
**输出：** 集团勘误移交原因说明校验处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收集团勘误移交原因说明校验相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.51 集团勘误移交原因说明写入（注：FPA功能点计数项）
**功能描述：** 将校验通过的集团勘误移交原因说明写入工单对应字段，更新工单状态为“勘误移交中”，记录写入日志。
**系统界面：** 无
**输入：** 集团勘误移交原因说明写入相关输入数据、业务规则配置、基础字典表数据
**输出：** 集团勘误移交原因说明写入处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收集团勘误移交原因说明写入相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.52 集团勘误移交派往对象匹配（注：FPA功能点计数项）
**功能描述：** 处理集团勘误移交派往对象匹配相关业务逻辑，接收输入数据，按预设规则处理，生成对应输出结果，支撑相关业务流程。
**系统界面：** 无
**输入：** 集团勘误移交派往对象匹配相关输入数据、业务规则配置、基础字典表数据
**输出：** 集团勘误移交派往对象匹配处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收集团勘误移交派往对象匹配相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.53 集团勘误移交派往对象修改（注：FPA功能点计数项）
**功能描述：** 接收集团勘误移交派往对象修改请求，校验修改权限，更新派往对象信息并记录修改日志。
**系统界面：** 无
**输入：** 集团勘误移交派往对象修改相关输入数据、业务规则配置、基础字典表数据
**输出：** 集团勘误移交派往对象修改处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收集团勘误移交派往对象修改相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.54 集团勘误移交受理时限处理（注：FPA功能点计数项）
**功能描述：** 计算集团勘误移交受理时限（从移交申请到受理的最大允许时间），监控受理进度，超时则触发告警。
**系统界面：** 无
**输入：** 集团勘误移交受理时限处理相关输入数据、业务规则配置、基础字典表数据
**输出：** 集团勘误移交受理时限处理处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收集团勘误移交受理时限处理相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.55 集团勘误移交处理时限处理（注：FPA功能点计数项）
**功能描述：** 定义集团勘误移交处理时限标准，监控处理进度，超时自动提醒处理人员，记录超时情况。
**系统界面：** 无
**输入：** 集团勘误移交处理时限处理相关输入数据、业务规则配置、基础字典表数据
**输出：** 集团勘误移交处理时限处理处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收集团勘误移交处理时限处理相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.56 集团勘误工单移交后当前工单处理组变更逻辑（注：FPA功能点计数项）
**功能描述：** 集团勘误工单移交后，按预设逻辑自动变更当前工单处理组（如从省中心处理组变更为地市处理组），更新处理组信息。
**系统界面：** 无
**输入：** 集团勘误工单移交后当前工单处理组变更逻辑相关输入数据、业务规则配置、基础字典表数据
**输出：** 集团勘误工单移交后当前工单处理组变更逻辑处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收集团勘误工单移交后当前工单处理组变更逻辑相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.57 集团勘误移交后多个时间的处理逻辑（注：FPA功能点计数项）
**功能描述：** 集团勘误移交后，处理多个关键时间（受理时间、处理时间、完成时间）的更新逻辑，确保时间数据一致性。
**系统界面：** 无
**输入：** 集团勘误移交后多个时间的处理逻辑相关输入数据、业务规则配置、基础字典表数据
**输出：** 集团勘误移交后多个时间的处理逻辑处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收集团勘误移交后多个时间的处理逻辑相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.58 新增上报量调控规则配置（注：FPA功能点计数项）
**功能描述：** 提供上报量调控规则新增功能，配置调控参数（专业类型、时间周期、阈值）、调控措施，保存规则配置。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 新增的规则配置数据（规则ID、配置参数、启用状态），入库至对应规则表
**处理过程：** 1. 接收规则配置参数；2. 校验参数合法性及操作人权限；3. 生成规则ID；4. 保存规则配置至规则表；5. 记录新增日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：新增上报量调控规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.59 修改上报量调控规则配置（注：FPA功能点计数项）
**功能描述：** 支持上报量调控规则的修改操作，更新调控参数、措施等配置信息，保存修改并记录规则变更日志。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 修改后的规则配置数据、规则变更日志，更新规则表对应记录
**处理过程：** 1. 接收规则修改请求及参数；2. 校验权限及参数合法性；3. 定位规则表对应记录；4. 更新规则配置参数；5. 记录修改日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：修改上报量调控规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.60 查询上报量调控规则配置（注：FPA功能点计数项）
**功能描述：** 接收上报量调控规则查询条件（规则ID、专业类型、启用状态），筛选规则数据，分页展示查询结果。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 匹配的规则配置列表，支持分页展示
**处理过程：** 1. 接收查询条件；2. 校验查询权限；3. 筛选规则表符合条件记录；4. 按指定顺序排序；5. 生成分页查询结果
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：查询上报量调控规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.61 月度统计当月每天预计上报量（注：FPA功能点计数项）
**功能描述：** 按自然月统计当月每天各专业的预计上报量，生成日度预计上报量报表，支持趋势分析。
**系统界面：** 无
**输入：** 统计维度条件（时间范围、专业类型、工单状态）、基础数据（工单数据、上报数据）、统计规则
**输出：** 统计结果报表（表格/图表）、统计数据明细，入库至对应统计报表
**处理过程：** 1. 接收月度统计当月每天预计上报量相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：月度统计报表
本期涉及原有但没修改的内部逻辑文件：事件工单主表、基础字典表、权限表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.62 分专业统计预计上报量（注：FPA功能点计数项）
**功能描述：** 按业务专业（无线、传输、核心网）统计预计上报量，生成专业维度上报量统计报表。
**系统界面：** 无
**输入：** 统计维度条件（时间范围、专业类型、工单状态）、基础数据（工单数据、上报数据）、统计规则
**输出：** 统计结果报表（表格/图表）、统计数据明细，入库至对应统计报表
**处理过程：** 1. 接收分专业统计预计上报量相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：分专业统计报表
本期涉及原有但没修改的内部逻辑文件：事件工单主表、基础字典表、权限表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.63 分小时统计预计上报量（注：FPA功能点计数项）
**功能描述：** 按小时粒度统计预计上报量，分析上报量时段分布特征，生成小时级上报量统计数据。
**系统界面：** 无
**输入：** 统计维度条件（时间范围、专业类型、工单状态）、基础数据（工单数据、上报数据）、统计规则
**输出：** 统计结果报表（表格/图表）、统计数据明细，入库至对应统计报表
**处理过程：** 1. 接收分小时统计预计上报量相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：分小时统计报表
本期涉及原有但没修改的内部逻辑文件：事件工单主表、基础字典表、权限表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.64 分专业小时统计上报量（注：FPA功能点计数项）
**功能描述：** 结合专业维度和小时粒度，统计各专业每小时的上报量，生成专业-小时二维上报量统计报表。
**系统界面：** 无
**输入：** 统计维度条件（时间范围、专业类型、工单状态）、基础数据（工单数据、上报数据）、统计规则
**输出：** 统计结果报表（表格/图表）、统计数据明细，入库至对应统计报表
**处理过程：** 1. 接收分专业小时统计上报量相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：分专业小时统计报表
本期涉及原有但没修改的内部逻辑文件：事件工单主表、基础字典表、权限表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.65 每5分钟定时筛选可上报数据（注：FPA功能点计数项）
**功能描述：** 每5分钟定时执行筛选任务，筛选符合上报条件的数据（如状态为“待上报”、在上报时段内），生成可上报数据列表。
**系统界面：** 无
**输入：** 每5分钟定时筛选可上报数据相关输入数据、业务规则配置、基础字典表数据
**输出：** 每5分钟定时筛选可上报数据处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收每5分钟定时筛选可上报数据相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.66 按照分专业当前可上报数量进行上报（注：FPA功能点计数项）
**功能描述：** 按业务专业统计当前可上报数量，根据上报量调控规则，控制各专业的上报数量，执行分专业上报操作。
**系统界面：** 无
**输入：** 按照分专业当前可上报数量进行上报相关输入数据、业务规则配置、基础字典表数据
**输出：** 按照分专业当前可上报数量进行上报处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收按照分专业当前可上报数量进行上报相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.67 按照对应数量进行分专业数据上报（注：FPA功能点计数项）
**功能描述：** 根据各专业的预计上报数量和当前可上报数量，按对应数量执行分专业数据上报，记录上报结果。
**系统界面：** 无
**输入：** 按照对应数量进行分专业数据上报相关输入数据、业务规则配置、基础字典表数据
**输出：** 按照对应数量进行分专业数据上报处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收按照对应数量进行分专业数据上报相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.68 工单超频次数统计（注：FPA功能点计数项）
**功能描述：** 按工单类型、专业统计超频上报次数，分析超频原因，生成工单超频次数统计报表。
**系统界面：** 无
**输入：** 统计维度条件（时间范围、专业类型、工单状态）、基础数据（工单数据、上报数据）、统计规则
**输出：** 统计结果报表（表格/图表）、统计数据明细，入库至对应统计报表
**处理过程：** 1. 接收工单超频次数统计相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：工单超频次数统计报表
本期涉及原有但没修改的内部逻辑文件：事件工单主表、基础字典表、权限表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.69 自然月超频管控上报判定（注：FPA功能点计数项）
**功能描述：** 统计自然月内工单上报数量，对比超频管控阈值，判定是否触发超频管控，决定是否限制上报。
**系统界面：** 无
**输入：** 自然月超频管控上报判定相关输入数据、业务规则配置、基础字典表数据
**输出：** 自然月超频管控上报判定处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收自然月超频管控上报判定相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.70 当前工单超频管控上报判定（注：FPA功能点计数项）
**功能描述：** 针对当前待上报工单，结合本月已上报数量和管控阈值，判定是否允许上报，生成上报许可结果。
**系统界面：** 无
**输入：** 当前工单超频管控上报判定相关输入数据、业务规则配置、基础字典表数据
**输出：** 当前工单超频管控上报判定处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收当前工单超频管控上报判定相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.71 新增上报白名单规则配置（注：FPA功能点计数项）
**功能描述：** 提供上报白名单规则新增功能，配置白名单条件（事件类型、设备类型、区域），保存白名单规则配置。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 新增的规则配置数据（规则ID、配置参数、启用状态），入库至对应规则表
**处理过程：** 1. 接收规则配置参数；2. 校验参数合法性及操作人权限；3. 生成规则ID；4. 保存规则配置至规则表；5. 记录新增日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：新增上报白名单规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.72 修改上报白名单规则配置（注：FPA功能点计数项）
**功能描述：** 支持上报白名单规则的修改操作，更新白名单条件、启用状态等信息，保存修改并记录变更日志。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 修改后的规则配置数据、规则变更日志，更新规则表对应记录
**处理过程：** 1. 接收规则修改请求及参数；2. 校验权限及参数合法性；3. 定位规则表对应记录；4. 更新规则配置参数；5. 记录修改日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：修改上报白名单规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.73 删除上报白名单规则配置（注：FPA功能点计数项）
**功能描述：** 接收上报白名单规则删除请求（规则ID），校验删除权限，删除规则配置并记录删除日志。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 规则删除结果（成功/失败）、删除日志，从规则表删除对应记录
**处理过程：** 1. 接收规则删除请求；2. 校验删除权限及规则引用情况；3. 定位规则表对应记录；4. 删除规则配置；5. 记录删除日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：删除上报白名单规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.74 查询上报白名单规则配置（注：FPA功能点计数项）
**功能描述：** 接收上报白名单规则查询条件（规则ID、事件类型、启用状态），筛选规则数据，分页展示查询结果。
**系统界面：** 无
**输入：** 规则配置参数（条件、阈值、优先级）、操作人权限信息、现有规则数据
**输出：** 匹配的规则配置列表，支持分页展示
**处理过程：** 1. 接收查询条件；2. 校验查询权限；3. 筛选规则表符合条件记录；4. 按指定顺序排序；5. 生成分页查询结果
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 2 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：查询上报白名单规则表
本期涉及原有但没修改的内部逻辑文件：操作人权限表、规则变更日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.75 重大故障白名单校验（注：FPA功能点计数项）
**功能描述：** 验证重大故障工单是否在白名单范围内，白名单内工单不受超频管控限制，允许正常上报。
**系统界面：** 无
**输入：** 重大故障白名单校验相关输入数据、业务规则配置、基础字典表数据
**输出：** 重大故障白名单校验处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收重大故障白名单校验相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.76 集团事件白名单校验（注：FPA功能点计数项）
**功能描述：** 验证集团事件工单是否属于白名单事件类型，白名单内事件工单可优先上报，不受常规管控限制。
**系统界面：** 无
**输入：** 集团事件白名单校验相关输入数据、业务规则配置、基础字典表数据
**输出：** 集团事件白名单校验处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收集团事件白名单校验相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.77 按专业日期上报量统计（注：FPA功能点计数项）
**功能描述：** 按业务专业和日期统计实际上报量，对比预计上报量，生成专业日期维度上报量统计分析。
**系统界面：** 无
**输入：** 统计维度条件（时间范围、专业类型、工单状态）、基础数据（工单数据、上报数据）、统计规则
**输出：** 统计结果报表（表格/图表）、统计数据明细，入库至对应统计报表
**处理过程：** 1. 接收按专业日期上报量统计相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：按专业日期上报量统计报表
本期涉及原有但没修改的内部逻辑文件：事件工单主表、基础字典表、权限表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.78 上报清单维护（注：FPA功能点计数项）
**功能描述：** 处理上报清单维护相关业务逻辑，接收输入数据，按预设规则处理，生成对应输出结果，支撑相关业务流程。
**系统界面：** 无
**输入：** 上报清单维护相关输入数据、业务规则配置、基础字典表数据
**输出：** 上报清单维护处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收上报清单维护相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.79 上报清单多条件查询（注：FPA功能点计数项）
**功能描述：** 接收上报清单查询条件（上报时间、专业类型、上报状态），多条件组合筛选上报清单数据，返回查询结果。
**系统界面：** 无
**输入：** 查询条件（ID、时间范围、状态）、基础数据表、权限校验信息
**输出：** 查询结果列表（含关键字段），支持分页展示、导出
**处理过程：** 1. 接收上报清单多条件查询相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：上报清单多条件统计报表
本期涉及原有但没修改的内部逻辑文件：事件工单主表、基础字典表、权限表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

###### 5.1.1.1.80 上报清单导出（注：FPA功能点计数项）
**功能描述：** 处理上报清单导出相关业务逻辑，接收输入数据，按预设规则处理，生成对应输出结果，支撑相关业务流程。
**系统界面：** 无
**输入：** 上报清单导出相关输入数据、业务规则配置、基础字典表数据
**输出：** 上报清单导出处理结果数据、相关业务记录，更新至对应业务表
**处理过程：** 1. 接收上报清单导出相关输入数据；2. 校验数据合法性及操作权限；3. 按业务规则处理数据；4. 生成处理结果；5. 更新对应业务表及记录日志
**本事务功能涉及到的数据文件（即FTR/RET）**
本事务功能预计涉及到 3 个内部逻辑文件，0 个外部逻辑文件
本期新增/变更的内部逻辑文件：无
本期涉及原有但没修改的内部逻辑文件：事件工单主表、相关字典表、操作日志表
本期新增/变更的外部逻辑文件：无
本期涉及原有但没修改的外部逻辑文件：无

